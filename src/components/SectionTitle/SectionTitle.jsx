
const SectionTitle = ({heading}) => {
    return (
        <div>
            <h3 className="font-bold mx-auto text-4xl text-center uppercase mt-10 mb-8">{heading}</h3>
        </div>
    );
};

export default SectionTitle;